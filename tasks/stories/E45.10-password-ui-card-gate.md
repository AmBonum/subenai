# E45.10 — UI: `PasswordCard` (Settings) + `RespondentPasswordGate` (`/t/$shareId`)

**Epic:** [E45](../PLAN-2026-05-21-E45-test-detail-editor.md)
**Effort:** `M`
**Status:** ✅ Done
**Commit prefix:** `feat(tests):`

## Akceptačné kritériá

- **AC-1:** `src/components/app/tests/PasswordCard.tsx` — karta v Settings
  tab `/app/tests/$testId`. Tri stavy podľa `hasPassword`:
  - `false`: heslo input + confirm + *Nastaviť heslo* button
  - `true`: nový-heslo input + confirm + *Zmeniť heslo* + *Zrušiť heslo*
- **AC-2:** Klient-side validácia: min 8 znakov + confirm match. RPC
  enforce-uje obe znova (security gate). Žiadny zxcvbn-lite v Phase 2.
- **AC-3:** Test-ids v namespace `test-editor-password-*`:
  `root`, `status` (s `data-has-password` atribútom), `input`, `confirm`,
  `hint`, `submit-button`, `clear-button`.
- **AC-4:** `src/components/respondent/RespondentPasswordGate.tsx` — modal-like
  card pred `TakeTestFlow` keď test má heslo. POSTuje na
  `/api/tests/verify-password`, na 200 volá `onVerified()`. Surface-uje
  Slovak error copy podľa response (401 / 429 / network).
- **AC-5:** `/t/$shareId` route má preflight hook `usePasswordPreflight`
  → GET `/api/tests/check-password?share_id=…`. Fail-open na 5xx (verify
  endpoint je real security gate; preflight je len UX skip).
- **AC-6:** i18n sk/en/cs: `password_gate.*` v `respondent-flow.json`,
  `editor.password.*` v `tests.json`. Slovenské reťazce verbatim z
  Appendix D § "Password gate UX copy".

## Definition of Done

- Lint 0/0
- `PasswordCard.test.tsx` (6 testov) + `RespondentPasswordGate.test.tsx`
  (5 testov) green.
- Existujúci `t-shareId.test.tsx` adaptovaný (preflight fetch mock,
  await na async render) — 4 testy green.
