# E45.19 — Fresh-context security review

**Epic:** [E45](../PLAN-2026-05-21-E45-test-detail-editor.md)
**Effort:** `M`
**Status:** ✅ Done
**Commit prefix:** `fix(security):`

## Context

Po merge Phases 1-3 + follow-up #97 na main spustený fresh-context
review subagentom `feature-dev:code-reviewer`. Zdrojom pravdy bola
`tasks/E45-appendix-A.md` — STRIDE T1-T14, rate-limit matrix,
JWT claim schema, cookie attrs, audit-log row schemas.

Plný report: [`tasks/E45-security-review-2026-05-21.md`](../E45-security-review-2026-05-21.md).

## Findings + fixes

| ID | Severity | Issue | Fix (this PR) |
|---|---|---|---|
| **M1** | MEDIUM | `target_id` v audit row pre unknown share používa `ipHash` namiesto `sha256(share_id)[:12]` (§5.2 contract drift). | Nový helper `hashShareId(shareId)` v `verify-password.ts`. `writeAuditRow` používa `unknown:${hashShareId(shareId)}` namiesto `unknown:${ipHash}`. Pokryté testom §M1 v `verify-password.test.ts`. |
| **M2** | MEDIUM | `wrong_issuer` JWT reason ticho prepadáva do `"no_cookie"` v `check-password.ts` (T1 observability gap). | Pridané do `bad_signature` vetvy v `check-password.ts`. Frontend videnie ostáva (user dostane výzvu zadať heslo), ale ops audit log teraz distinguish-uje T1 forgery od first-visit. |
| **L1** | LOW | `test_question_modified` audit action (§5.4) nikdy nezapísaný — Phase 1 queries layer je čisto client-side, žiadny server fn wrapper. | Nová migrácia `20260521230000_test_question_modified_audit.sql` — Postgres trigger `log_test_question_modified` na `test_questions` INSERT/DELETE/UPDATE. SECURITY DEFINER, REVOKE'd, používa `auth.uid()` ako actor. |
| **L2** | LOW | Rate-limit eval order v `verify-password.ts` je L3→L2→L1, Appendix A §3 hovorí L1→L2→L3. No security impact (equivalent properties). | Žiadny kód-change; Appendix A bude updatovaný v close-out PR. |

## Akceptačné kritériá

- **AC-1:** Review dokument existuje na `tasks/E45-security-review-2026-05-21.md`.
- **AC-2:** Žiadne HIGH findings.
- **AC-3:** Všetky MEDIUM findings (M1, M2) addressed v Phase 4.
- **AC-4:** L1 addressed via Postgres trigger; L2 dokumentovaný ako
  appendix drift bez kód-change.
- **AC-5:** Nový test pre §M1 fix v `tests/functions/verify-password.test.ts`
  (verifikuje že audit `target_id` pre unknown share používa share-id
  hash, nie IP hash).

## Definition of Done

- 1997 testov green (+2 nové z §M1 fix)
- Migrácia 20260521230000 mirror v DEPLOY_SETUP.sql
- Migráciu spustí používateľ na prod Supabase po merge tohto PR
