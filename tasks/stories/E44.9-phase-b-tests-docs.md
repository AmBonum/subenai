# E44.9 — Testy + dokumentácia pre Phase B

**Epic:** [E44 — Template Marketplace](../PLAN-2026-05-20-E44-template-marketplace.md)
**Effort:** `M` (1 deň)
**Priority:** `P2`
**Status:** ✅ Done (2026-05-21)
**Branch:** `feature/E44-phase-b`
**Commit prefix:** `test(templates):` / `docs(templates):`

## Context

Phase B doručuje server-side AI moderátor + klient submission flow. Story
E44.9 pokrýva: kontrakt migrácie cez regex nad SQL string, unit testy
nad `precheckSchema` a `derivePrecheckPassed` brány, unit testy nad
Anthropic helperom (cost, parse, retry, fallback), kontrakt testy nad
CF Function (auth, rate-limits, budget, ownership, cooldown, happy path,
fallback path) a prompt-injection testy nad A1–A5 z Appendix C § 5.

## Akceptačné kritériá

- **AC-1:** `tests/db/template_submissions.test.ts` — regex nad
  migráciou aj `DEPLOY_SETUP.sql` zrkadlom: enum 4 hodnoty, tabuľka 14
  stĺpcov, 3 indexy, 2 triggery, 4 RLS politiky, state-machine prechody.
- **AC-2:** `tests/lib/templates/precheck-schema.test.ts` — zod accept
  na 3 few-shot výstupy z Appendix C; reject pre out-of-range scores,
  chýbajúci summary, summary > 400 znakov, unknown age. Hranice
  `derivePrecheckPassed`: safety 0.29 vs 0.30, profanity 0.49 vs 0.50,
  empty vs non-empty copyright, age `16+` vs `18+`, `prompt_injection`
  v categories vždy false. `stripRaw` + `toVerdict` shape.
- **AC-3:** `tests/functions/anthropic.helper.test.ts` — `costUsd`
  s konkrétnym číslom; `tryParseResult` happy / json-fail / schema-fail;
  `manualReviewFallback` shape; `callPrecheckWithRetry` happy, retry-success,
  retry-fail-throws, 429-no-retry-throws, timeout-throws.
- **AC-4:** `tests/functions/templates-precheck.contract.test.ts` —
  401 bez authu, 400 bad body, 429 per-IP/user/global, 503 budget,
  404 not-owned, 429 cooldown, 200 happy, 200 fallback. Verifikuje
  Supabase insert/update/audit shapes. Stub `globalThis.fetch` pre
  Anthropic, chainable Supabase mock.
- **AC-5:** `tests/functions/templates-precheck.injection.test.ts` —
  5 adversarial vstupov A1–A5 (override, system impersonation, fake
  JSON, RTL Unicode, polite social engineering). Test mock-uje
  Claude verdict ako pravidlo-správne flagging `prompt_injection`,
  potom assertuje že deterministic gate vráti `precheck_passed=false`
  + `held_for_admin_review=true`. Real-Claude testy proti A1–A5 sú
  out-of-scope (potrebovali by live API key v CI).
- **AC-6:** Všetky testy sú v angličtine; Slovak ONLY ako verbatim
  UI string v assertion (žiadne v tomto súbore lebo precheck nemá
  user-facing copy okrem dialógu z E44.8 testovaného inde).
- **AC-7:** `npm run lint` 0/0; žiadny `.skip` ani `.only`; žiadny
  `console.log` v test súboroch.
- **AC-8:** Štyri story files (E44.6-9) existujú podľa štruktúry
  exemplárov (E44.1-5); CHANGELOG `[Unreleased]` má Phase B bullet
  v `### Pridané`.

## Subtasks

### Implementácia
- [x] 5 test súborov podľa AC-1 až AC-5 (celkom 106 testov: 42+24+16+14+10).
- [x] 4 story files (E44.6, E44.7, E44.8, E44.9).
- [x] CHANGELOG `[Unreleased]` Slovak bullet pre Phase B.

### Testy
- [x] Sám seba — `npm test` celá suite 1781/1781 zelená (1 skipped
      z E3.3 share flow, nesúvisí s E44).

### Dokumentácia
- [x] Story files.
- [x] CHANGELOG.

### Code review
- [ ] Čerstvý kontext — pri PR-B. Pozrieť kvalitu mockov (chainable
      Supabase) a či injection testy reálne testujú deterministickú
      bránu (nie iba shape mocku).

## Závislosti

- Vyžaduje E44.6, E44.7, E44.8 (testy proti shippnutej implementácii).
- Blokuje PR-B merge — DoD § 2.2 vyžaduje testy pred mergom.
