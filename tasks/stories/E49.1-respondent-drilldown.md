# E49.1 — Respondent drill-down: queries + side-sheet route + Results tab v2

**Status:** ✅ Done — 2026-05-22 — entire Phase 1 complete across two commits: `b9fc9ed` (Phase 1a: queries, side-sheet, Results v2, i18n, plan markdown) + Phase 1b (CSV export CF + Playwright POM/spec + injection contract battery).
**Plan:** `tasks/PLAN-2026-05-22-E49-tests-respondent-ops.md`
**Branch:** `feature/E49-tests-respondent-ops` (currently on worktree branch `claude/quizzical-buck-a631d3`)

## Goal

Close the missing post-publish surface on `/app/tests/$testId`: the test author must be able to see exactly who responded, how they scored, and what they answered question-by-question — without leaving the editor.

## Acceptance criteria

- New queries in `src/lib/platform/queries.ts`:
  - `useTestSessions(testId, { page, pageSize, sort, status, search })` — server-paginated; query key `["user","tests",testId,"sessions",{…}]`.
  - `useTestSessionAnswers(sessionId)` — joins `session_answers` rows with their `questions` metadata.
- New types in `src/lib/platform/types.ts`: `SessionAnswerWithQuestion`, `TestSessionsPage`.
- New file-based route `src/routes/app.tests.$testId.sessions.$sessionId.tsx` mounting `SessionDetailSheet` as a Radix Sheet overlay; closing navigates back to `/app/tests/$testId?tab=results`.
- New components `src/components/app/tests/SessionsList.tsx` + `src/components/app/tests/SessionDetailSheet.tsx`.
- Results tab in `src/routes/app.tests.$testId.tsx` rewritten: 3 KPI cards (total, completed, avg score) + `SessionsList`. Parent renders `<Outlet />` so child route mounts portal'd above the editor.
- i18n: new `editor.*` filter / sort / pagination / KPI strings, and a new `session_detail` namespace across `sk/en/cs`.
- Every interactive/semantic DOM element has a `data-testid` per CLAUDE.md naming rule (`test-sessions-list-*`, `session-detail-*`, `test-editor-kpi-*`).
- Vitest:
  - `tests/lib/platform/sessions-queries.test.ts` — query-chain shape, pagination, sort, status filter, search escape, answers join behaviour with missing question metadata.
  - `tests/components/app/tests/sessions-list.test.tsx` — empty state, loading, filter change, row open link.
- Playwright test plan: `specs/app/test-sessions-detail.md` — full TC inventory, ready for spec generation in E49.7.
- Lint 0/0, full vitest green except the 2 pre-existing `tests/functions/attachment-*` failures already red on `main`.

## Implementation notes

- KPI uses two scoped `useTestSessions` queries (pageSize 1 for `total`, pageSize 100 for `completed`). Aggregate RPC remains a follow-up if author scale exceeds 100 completed sessions on a single test.
- Search term sanitizes `%,(,)` before interpolating into PostgREST `or(intake_data->>name.ilike, intake_data->>email.ilike)` to avoid syntax break.
- `SessionAnswerWithQuestion.question.correct` is typed `unknown` — UI stringifies non-string shapes defensively.
- Parent route adds `<Outlet />` at the end; existing unit test in `tests/routes/app/test-editor.test.tsx` extends its router mock with `Outlet: () => null` for isolation.
- Side-sheet handles three states: loading skeleton → 404 (`session-detail-not-found`) → rendered detail. Empty-answers state for `in_progress` sessions.

## Phase 1b deliverables (commit 2 — CSV export + Playwright + injection battery)

- `functions/api/tests/export-sessions.ts` — owner-only CSV export GET endpoint. Same-origin enforcement, Supabase JWT auth, per-IP-per-hour (100) + per-author-per-day (50) rate limits, owner check, 5000-row cap with `X-Total-Capped` header, UTF-8 BOM, RFC-4180 quoting, leading-quote injection escape on cells starting with `= + - @ \t \r`. Audit row per export with `action='test.export_csv'`, `pii_access=true`.
- `functions/_lib/csv.ts` — pure helper module (`escapeCsvCell`, `serializeCsvRow`, `serializeCsv`). Reused by tests for the injection contract.
- `tests/functions/export-sessions.test.ts` — 14 tests: 401/403/404, Origin/Referer check, BOM bytes, audit row contents, rate limit, capping, injection escape, helper functions.
- `tests/security/csv-export-injection.test.ts` — payload-battery contract using existing `tests/security/e48-payloads.ts` (XSS + CSV injection). Round-trip via in-test RFC-4180 parser.
- `src/components/app/tests/SessionsList.tsx` — Export CSV button next to page-size selector. Disabled when no sessions or pending. Blob download via object URL.
- `src/lib/platform/queries.ts` — `useExportTestSessionsCsv(testId)` mutation.
- `src/i18n/locales/{sk,en,cs}/tests.json` — `editor.export_csv_{button,success,error}`.
- `e2e/poms/app/TestSessionsDetail.ts` — `TestSessionsDetailPage` POM. List + KPI + side-sheet getters, parameterized answer-row getters, navigation/filter/sort/pagination actions.
- `e2e/specs/app/test-sessions-detail.spec.ts` — 19 TCs (6 happy + 6 side-sheet + 4 empty + 1 IDOR + 1 a11y + 1 `@mobile`). POM-only locator discipline.
- `e2e/seed/sessions.ts` + `e2e/seed/index.ts` — `seedE49TestWithSessions` helper. Two test ownerships (own + foreign for IDOR), six sessions covering the precondition matrix from `specs/app/test-sessions-detail.md`.
- Test-id rename in `SessionDetailSheet.tsx` to match the agreed plan contract (`session-detail-{root,respondent-identity,audit-ref,empty-in-progress,close,answer-row-<qid>-{value,expected,correctness,time}}`). All 934 surrounding tests still green.

## DoD

- [x] Implementation in `src/**` + `functions/**` + `e2e/**`.
- [x] `npm run lint` → 0 errors / 0 warnings.
- [x] `npm test` → 934 passed / 11 skipped on the app + functions + security suites we touched. The 2 pre-existing `tests/functions/attachment-*` failures are red on `main` (verified by `git stash` smoke).
- [x] `npm run build` → ✓.
- [x] CHANGELOG entries (drill-down + CSV export).
- [x] Plan checklist marked for E49.1–E49.8.
- [ ] Story marked Done in `tasks/FEATURE_MAP.md` — N/A: feature map row tracked by epic, not per-story.
- [ ] Privacy / cookies updated — N/A: no new data surface (display-only + author-only export of existing columns, audit-logged).
- [ ] Playwright suite run end-to-end against a live env — DEFERRED to E49 final integration (depends on Phase 2 RPCs landing; the mock supabase route in `e2e/seed/sessions.ts` carries 3 caveats flagged in the planner agent's report, which Phase 2's real RLS will resolve).
