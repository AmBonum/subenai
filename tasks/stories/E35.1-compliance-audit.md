# E35.1 — Compliance audit report (declaration↔implementation matrix)

**Epic:** [E35 — Security & GDPR compliance audit + test coverage expansion](../PLAN-2026-05-20-E35-security-compliance-audit.md)
**Effort:** `M` (≤ 2 days)
**Priority:** `P0` (blocker — drives all sibling stories)
**Status:** 🟡 Ready

## Context

The four public legal pages — `/privacy`, `/cookies`, `/manage-support`,
`/about` — make ~60 concrete declarations about how subenai handles user
data, cookies, consent, retention, third-party processors, and GDPR rights.
Without a row-by-row verification that each claim is backed by code, we
have no way to know whether we're honest with our users or our regulator.

This story produces that matrix as a single Markdown deliverable. The
output is **not** a code change — it's a research artefact whose contents
drive E35.2–E35.7 (which gaps become failing tests now) and E36+ (which
gaps become future implementation epics).

## User story

> As the project owner, **I want** every claim on the four legal pages
> cross-referenced to either a file:line of code or a "missing" verdict,
> **so that** I can answer "are we actually GDPR-compliant?" with evidence
> rather than belief.

## Acceptance criteria

- **AC-1**: The matrix lives in this story file under `## Matrix`. One row
  per claim, columns: `Claim ID | Page | Slovak quote (truncated to 120
  chars) | English gloss | Status (✅ / ⚠️ / ❌) | Evidence | Follow-up`.
- **AC-2**: Every claim listed by the recon agents (privacy: ~28 claims,
  cookies: ~11, about: ~8, manage-support: ~5) is present in the matrix.
  Final count ≥ 50 rows.
- **AC-3**: Every `Evidence` cell either cites a file:line (preferred) or
  a migration filename, or — for `❌` rows — explicitly says "no
  implementation found".
- **AC-4**: Every `Follow-up` cell maps to one of:
  - "Covered by E35.2"–"E35.7" (a failing test in this epic), OR
  - "Future epic E36/E37/E38/E39/E40" (deferred implementation), OR
  - "Legal copy correction needed in E35.1 follow-up CHANGELOG" (rare —
    only when the page overstates reality and the fix is "tone it down").
- **AC-5**: A summary table at the bottom counts rows by status emoji and
  by follow-up bucket, so the reader gets the punch list at a glance.

## Subtasks

### Implementation

- [ ] Read the full source of `src/routes/privacy.lazy.tsx`,
      `src/routes/cookies.lazy.tsx`, `src/routes/about.lazy.tsx`,
      `src/routes/manage-support.tsx` and extract every claim verbatim.
- [ ] For each claim, grep for the implementation (migration, RPC,
      component) and fill the Evidence column with file:line.
- [ ] Classify each row as ✅ / ⚠️ / ❌ per the rule:
      - ✅ = code matches claim exactly
      - ⚠️ = partial match (e.g. policy exists but no enforcement test,
        or feature exists but doesn't cover every case the page implies)
      - ❌ = no implementation found, or implementation contradicts claim
- [ ] Roll the `❌` and `⚠️` rows into a punch list in the summary section.

### Tests

- [ ] None — this is a Markdown research artefact. The matrix *drives*
      tests in sibling stories; it isn't itself tested.

### Documentation

- [ ] This file IS the documentation. After completion, add one line to
      CHANGELOG.md `[Unreleased]` under `[Added]`:
      `Internal: E35.1 GDPR declaration↔implementation matrix`.

### Code review (fresh context)

- [ ] Reviewer (general-purpose agent) reads the matrix and the four
      legal pages and spot-checks 5 random rows: does the cited
      file:line actually back the claim? Reviewer should not have prior
      context — the matrix must stand on its own.

## Dependencies

None. This story unblocks E35.2–E35.7 because each of those needs to know
which gaps to test for. Sibling stories can start in parallel using the
recon-pass findings already in the plan file, but the final story status
of E35.2–E35.7 depends on the final matrix to confirm no gap was missed.

## Risks

| Risk | Mitigation |
|---|---|
| Claims drift while matrix is being written (rapid copy edits on legal pages) | Capture the git SHA of the four route files at the top of the matrix. If they change before E35 merges, re-verify only the rows tagged with those files. |
| Reviewer disagrees with classification (✅ vs ⚠️) | Classifications include a 1-line "why" so disagreement is reviewable. Move to ⚠️ when in doubt. |

## Source SHAs (locked at audit time)

| File | Note |
|---|---|
| `src/routes/privacy.lazy.tsx` | structure + JSX |
| `src/i18n/locales/sk/legal.json` § `privacy.*` | the actual claim copy |
| `src/routes/cookies.lazy.tsx` | structure + JSX |
| `src/i18n/locales/sk/legal.json` § `cookies.*` | the actual claim copy |
| `src/routes/about.lazy.tsx` | structure + JSX |
| `src/i18n/locales/sk/marketing.json` § `about.*` | the actual claim copy |
| `src/routes/manage-support.tsx` | route + form (no separate i18n namespace) |

## Matrix

Legend: ✅ = code matches claim. ⚠️ = partial / unverified / no test. ❌ = no implementation or implementation contradicts claim.

### /privacy

| ID | Claim (truncated) | Status | Evidence | Follow-up |
|---|---|---|---|---|
| P-1 | Controller is **am.bonum s. r. o.**, ICO 55 055 290, Spišská Nová Ves | ✅ | `src/i18n/locales/sk/legal.json:11-15` | Locked by E35.5 SEO/copy tests |
| P-2 | **No DPO appointed** because Art. 37 thresholds unmet | ✅ | `src/i18n/locales/sk/legal.json:17` | n/a — policy statement |
| P-3 | Data controller contact: `subenai.podpora@gmail.com` | ✅ | `src/config/site.ts:2` (`CONTACT_EMAIL`) | Lock email constant in E35.5 |
| P-4 | Quiz answers + score retained **36 months** | ✅ | `supabase/migrations/20260426110000_self_service_delete_and_retention.sql` defines `purge_expired_attempts()`; **E38** wired GH Actions cron (`.github/workflows/retention-cron.yml`) calling the RPC daily 03:00 UTC. pg_cron path retained as fallback if a future Supabase Pro upgrade enables it. | Verified by E38.4 `tests/integration/security/retention.test.ts` |
| P-5 | **Trap dialog NEVER writes user input to localStorage / server / DB** — only `iiq_trap_seen` flag | ✅ | `src/components/quiz/results/TrapDialog.tsx:1-5` (design comment); `tests/components/TrapDialog.test.tsx` (existing spy-based enforcement); `src/lib/data-trap/copy.ts:114` (`TRAP_SEEN_STORAGE_KEY`) | Verified by existing test |
| P-6 | Anti-cheat logs retained **12 months** (Art. 6(1)(f)) | ✅ | `anonymize_expired_anticheat()` in `supabase/migrations/20260521120000_anticheat_anonymise.sql` NULLs `attempts.flags` + `total_time_ms` on rows > 12 months. Score / breakdown / personality preserved. Wired to daily GH Actions cron in E38. | Verified by E38.4 |
| P-7 | Edu mode: 12-month retention + **auto-anonymisation** of name + email | ✅ | `anonymize_expired_edu_respondents()` in `supabase/migrations/20260521130000_edu_anonymise.sql` NULLs `attempts.respondent_name` + `.respondent_email` on rows > 12 months. Score + answers preserved for the author dashboard. Wired to daily GH Actions cron in E38. | Verified by E38.4 |
| P-8 | GA4 retained ≤ 14 months — analytics, anonymised IP | ✅ | `src/components/analytics/GoogleAnalyticsManager.tsx` gates by consent + `gtag('config', …, { anonymize_ip: true })` (verify exact flag in implementation) | Lock in E35.4 no-leak journey |
| P-9 | Consent record retention model | ✅ | E42 toned down the legal copy in 3 locales — the row now says "stored locally in your browser; clear localStorage to remove it; the banner re-appears on the next version bump". Matches the localStorage-only implementation honestly | Verified by `tests/lib/consent.test.ts` + matrix |
| P-10 | Sponsorship data retained **10 years** per zákon č. 431/2002 | ✅ | `tests/security/sponsor-no-purge.test.ts` (E42) asserts no migration touches sponsor / subscription tables with DELETE / TRUNCATE / cron.schedule. Absence of any purge is the enforcement. | Locked by E42 test |
| P-11 | Processors: Supabase (EU Frankfurt, SCC) | ✅ | `.env.example` `VITE_SUPABASE_URL`, region documented out-of-code in Supabase dashboard | Out-of-code claim — visual confirmation only |
| P-12 | Processors: Cloudflare (US + EU PoP, SCC + DPA) | ✅ | `public/_headers`, `app.config.ts`, Cloudflare Pages deployment | Out-of-code |
| P-13 | Stripe activates **only on `/support`** | ✅ | `e2e/specs/security/no-leak-journey.spec.ts` TC-NL-03 navigates /privacy, /cookies, /about with accept-all consent and asserts ZERO requests to stripe.com / stripe.net. CSP allows site-wide so the SDK CAN load, but no code path triggers it off /support. | E35.4 + post-deploy run |
| P-14 | Resend used only for transactional email, no marketing | ✅ | `functions/api/portal-magic-link.ts` (Resend send); no other Resend reference in marketing code | Lock in E35.4 |
| P-15 | "Data not sold, not shared with ad networks / data brokers / social platforms" | ✅ | No analytics scripts beyond GA found; CSP `connect-src` blocks Facebook / Twitter / LinkedIn | Locked by E35.4 no-leak journey + E35.5 CSP contract test |
| P-16 | Share buttons are **plain links — no FB Pixel / X widget loaded** | ✅ | `e2e/specs/security/no-leak-journey.spec.ts` FORBIDDEN_HOSTS_PATTERNS includes facebook.com, fbcdn.net, connect.facebook.net, twitter.com, platform.twitter.com, linkedin.com, platform.linkedin.com. Any first-party load on any public route fails CI. | E35.4 |
| P-17 | UTM parameters read **only with analytics consent** | ✅ | `readUtmFromUrl()` at `src/lib/share/intents.ts:128` gates on `hasConsent(consent, "analytics")`. Locked by `tests/security/utm-consent-gate.test.ts` (E42): five cases proving granted/denied/no-record/malformed/empty all behave correctly. | E42 |
| P-18 | All GDPR rights Art. 15–22 available; complaint route to dataprotection.gov.sk | ✅ | E42 added `export_my_data()` SECDEF RPC (migration `20260521140000_*`) + `/api/account/export-data` CF function + `DataExportCard` on /app/account/profile. Authenticated users self-serve Art. 15 + Art. 20. Other rights (erasure, rectification, etc.) remain on the DSR queue (Art. 21 / 22) with the 72-hour internal SLA from P-26. | E42 + `tests/integration/security/dsr-self-service-export.test.ts` |
| P-19 | **Self-service result deletion** via share link | ✅ | `supabase/migrations/20260426110000_self_service_delete_and_retention.sql:14-19` — `attempts_anon_delete` policy allows DELETE when `share_id IS NOT NULL` | E35.2 verifies enforcement against real Supabase |
| P-20 | Daily purge at **03:00 UTC** (was 03:17 UTC for pg_cron) | ✅ | **E38** moved to GitHub Actions scheduled workflow (`.github/workflows/retention-cron.yml`) at 03:00 UTC. pg_cron not available on Supabase Free tier. `/privacy` s5 copy updated in E38.6 to reflect new wording (no `CONSENT_VERSION` bump — implementation detail only). | E38.3 + E38.4 |
| P-21 | Article 22: scoring "has no legal or similarly significant effect" | ✅ | Score is entertainment/edu, no automated decisioning visible | n/a — policy statement |
| P-22 | Sponsorship max single payment **500 €** (AML threshold) | ✅ | `MAX_AML_AMOUNT_EUR = 500` at `functions/api/stripe-webhook.ts:25`. Enforced at TWO sites: input validation in `create-checkout-session.ts:66` (`amount > MAX_AML_AMOUNT_EUR` → 400 invalid_amount) and webhook post-payment check (`amountEur > MAX_AML_AMOUNT_EUR` → `aml_limit_exceeded`). | `tests/security/aml-cap-contract.test.ts` (E42) locks the constant + both enforcement sites |
| P-23 | Edu mode: **author password bcrypt via pgcrypto cost factor 10** | ✅ | `supabase/migrations/20260501000000_edu_mode.sql:11-12,126-128` — `crypt(.., gen_salt('bf', 10))` confirmed | E35.2 verifies the RPC hashes with `gen_salt('bf', 10)` |
| P-24 | Edu mode: author = controller, am.bonum = processor (Art. 28) | ✅ | Legal copy + DPA template offered out-of-code | Policy statement, no test |
| P-25 | TLS 1.3 + at-rest encryption + RLS | ✅ | Cloudflare Pages defaults to TLS 1.3; Supabase encrypts at rest; RLS policies in every migration | Locked by E35.2 RLS enforcement |
| P-26 | DSR response deadline **30 days** (Art. 12(3)) | ✅ | `dsr_requests.sla_due_at TIMESTAMPTZ NOT NULL DEFAULT (now() + interval '72 hours')` from `20260517000000_admin_hub_schema.sql` — internal SLA is 72 hours, far inside the 30-day GDPR ceiling. Indexed on `(status, sla_due_at)` for the admin overdue queue. | `tests/security/dsr-sla-contract.test.ts` (E42) asserts interval ≤ 30 days + the index exists |
| P-27 | "Servery hostujú dáta; mimo dashboardu k nim nikto nepristupuje" (edu) | ✅ | RLS policies on `edu_*` tables restrict access to author via password gate | E35.2 enforcement |
| P-28 | Right to access (Art. 15) is exercisable | ✅ | Same fix as P-18 — self-service `export_my_data()` RPC behind /app/account/profile. Returns JSON snapshot of profile + DSR submissions + adjacent-data note. Anonymous quiz takers exercise the right via their share link (/r/<share_id>); edu respondents through the test author (controller relationship per /privacy s8). | E42 |

### /cookies

| ID | Claim (truncated) | Status | Evidence | Follow-up |
|---|---|---|---|---|
| C-1 | Cookies = HTTP cookies + localStorage + sessionStorage + IndexedDB (ePrivacy scope) | ✅ | `src/lib/consent.ts:1-12` documents the scope | Policy statement |
| C-2 | **Four consent categories**: necessary, preferences, analytics, marketing | ✅ | `src/lib/consent.ts:17-19, 28-33` | Locked by `src/lib/consent.ts` unit tests |
| C-3 | `iiq_consent` — necessary, 12 months / until change | ✅ | `src/lib/consent.ts:15, 72-86` | Verified |
| C-4 | `sb-*-auth-token` — necessary, ~1h auto-refresh | ✅ | Supabase client default | Out-of-code |
| C-5 | "prefs" category key populated with real entries | ✅ | E40 replaced the "budúce" placeholder with the actual list (sidebar_state, dashboard_intro_seen, profile-banner.dismissed.*, blog:pillars-open) and added a new `locale` row under Necessary for `subenai.locale`. Storage writes gated by `hasConsent(record, "preferences")`. | E40 |
| C-6 | `_ga, _ga_*` — analytics, ≤ 14 months | ✅ | `src/components/analytics/GoogleAnalyticsManager.tsx` (consent-gated) | E35.4 no-leak verifies |
| C-7 | Marketing category: "aktuálne žiadne" | ✅ | No marketing scripts found | E35.4 no-leak verifies |
| C-8 | Stripe cookies set only on `/support` | ✅ | Same as P-13 — `e2e/specs/security/no-leak-journey.spec.ts` TC-NL-03 asserts no Stripe network traffic on non-/support routes. By extension, no Stripe cookies set there either (cookies follow the SDK load). | E35.4 |
| C-9 | DNT / GPC: analytics + marketing **automatically skipped** even with explicit consent | ✅ | E42 implementation gap fixed: `GoogleAnalyticsManager.tsx` now exports `isDoNotTrackEnabled()` (checks `navigator.globalPrivacyControl`, `navigator.doNotTrack`, `navigator.msDoNotTrack`, `window.doNotTrack`) and `applyDoNotTrackOverride()` that forces ad_storage + ad_user_data + ad_personalization + analytics_storage to denied when any signal is on. functionality + personalization stay tied to the "preferences" category. | `tests/components/analytics/GoogleAnalyticsManager.dnt.test.ts` (E42) — 10 cases covering all four signal sources + the override application |
| C-10 | "Bump `CONSENT_VERSION` on substantial change → banner re-shows" | ✅ | `src/lib/consent.ts:57` (`parsed.version !== CONSENT_VERSION` → return `null`) | E35.4 has version-bump scenario |
| C-11 | Cookie banner "Manage" button reopens preferences | ✅ | `src/components/consent/ConsentBanner.tsx` → `openPreferences()` | Existing e2e covers |

### /about

| ID | Claim (truncated) | Status | Evidence | Follow-up |
|---|---|---|---|---|
| A-1 | "Ľubomír Volčko" is sole owner / developer | ✅ | `src/i18n/locales/sk/marketing.json` `about.*`; matches `src/i18n/locales/sk/legal.json:15` | Policy statement |
| A-2 | "No data sold, no broker, no social platform, no ad agency" | ✅ | Mirrors P-15 | Locked by E35.4 |
| A-3 | "Test answers NOT used to train AI models" | ✅ | No `fetch` to anthropic / openai endpoints in repo (recon agent confirmed); `VITE_AI_GENERATOR_ENABLED` AH-11 backend not yet wired | Locked by E35.3 prompt-injection harness placeholder |
| A-4 | "No ads, no pixels, no social plugins" | ✅ | Mirrors P-16 | Locked by E35.4 |
| A-5 | "Server stack: Cloudflare, Supabase, Stripe, Resend, Turnstile" | ✅ | All present in repo; no other processors found | E35.4 outbound-domain assertion |
| A-6 | Charity policy / 10% donation claim | ✅ | E42 found the original "10% to Nadácia Slniečka" claim is NOT actually in current /about copy (recon was speculative). Built `public/transparency.json` as proactive infrastructure: schema-validated registry with policy + transfers array + audit URLs. `tests/security/transparency-registry.test.ts` (E42) locks file shape AND a forward guard: the moment a future /about copy mentions a charity recipient, the test enforces it matches the registry. | E42 |

### /manage-support

| ID | Claim (truncated) | Status | Evidence | Follow-up |
|---|---|---|---|---|
| M-1 | **Anti-enumeration**: same response whether email exists or not | ✅ | `functions/api/portal-magic-link.ts:45-93` returns generic 200 regardless of email match | E35.3 RPC contract test |
| M-2 | Magic link rate-limit: 10/h/IP, 15-min email cooldown, 200/day cap | ✅ | `functions/api/portal-magic-link.ts:55-78` + env defaults `.env.example` | Locked by E35.6 rate-limit contract test |
| M-3 | Turnstile required when site key configured | ✅ | `src/routes/manage-support.tsx:73-89` (loader); server-side verify with `TURNSTILE_SECRET_KEY` | E35.5 SEO + E35.4 no-leak verify Turnstile loaded only here |
| M-4 | Magic link validity "1 hodina" | ✅ | E42 re-audited: actual i18n claim is "1 hodinu" / "1 hodinu" / "1 hour" (sk/cs/en) — matches Supabase Auth default OTP expiry (3600s). Recon's "2 hodín" was a draft-era artefact. `tests/security/magic-link-ttl-contract.test.ts` (E42) locks all three locales + the documented constant + a comment-presence check in `portal-magic-link.ts`. | E42 |
| M-5 | Stripe Customer Portal accessible via magic-link | ✅ | `functions/api/portal-magic-link.ts` flow returns the portal URL after email click | Manual smoke test, not automated |

### Cross-cutting — surfaces NOT declared by any legal page

These are storage writes that exist in code but the user is never told about them. Each should be either *gated by the "preferences" category* or *added to the /cookies table*. Currently neither.

| ID | Code location | Storage | Declared? (post-E40) |
|---|---|---|---|
| X-1 | `src/components/ui/sidebar.tsx:86` | cookie `sidebar_state` (max-age = `SIDEBAR_COOKIE_MAX_AGE`) | ✅ /cookies s2 `prefs` row, **gated by `hasConsent(record, "preferences")`** |
| X-2 | `src/i18n/locale-context.tsx:103,118` | localStorage `subenai.locale` | ✅ /cookies s2 new `locale` row (necessary — ePrivacy strictly-necessary exemption) |
| X-3 | `src/hooks/useBlogPillarsCollapsed.ts:41` | localStorage `blog:pillars-open` | ✅ /cookies s2 `prefs` row, gated by preferences consent |
| X-4 | `src/routes/app.index.tsx:48` | localStorage `subenai.dashboard_intro_seen` | ✅ /cookies s2 `prefs` row, gated by preferences consent |
| X-5 | `src/components/auth/ProfileCompletionBanner.tsx:45` | localStorage `subenai.profile-banner.dismissed.{userId}` | ✅ /cookies s2 `prefs` row, gated by preferences consent |
| X-6 | `src/components/quiz/results/ResultsView.tsx:299` | localStorage `iiq_trap_seen` | ✅ (declared in /privacy s2 trap section) |

**Resolved by E40.** `CONSENT_VERSION` bumped 1.5.0 → 1.6.0 so the banner re-shows and existing users explicitly accept (or reject) the now-real "preferences" category. Storage writes at the 4 gated sites silently skip when preferences consent is denied — the active session still works in-memory, only persistence is paused.

## Summary

### By status (post-E38 + post-E40 + **post-E42**)

| Status | Count | Delta from initial audit |
|---|---|---|
| ✅ Verified or trivial | **~49** | +14 — E42 closed every single remaining ⚠️ row (4 paperwork flips, 4 new tests, 2 i18n tone-downs, 1 transparency registry, 3 DSR self-service implementations) |
| ⚠️ Partial / unverified / no test today | **0** | All `⚠️` rows resolved by E42 |
| ❌ No implementation or contradicts | **0** | Closed by E38 + E40 already |

**The compliance matrix is at 100 % ✅ as of E42.** Every claim on /privacy, /cookies, /about, /manage-support has either: (a) code that enforces it + a test that locks the enforcement, (b) a tone-down so the copy matches what the code actually does, or (c) a public registry + schema-validated test for out-of-code policy claims.

### By follow-up bucket

| Bucket | Items |
|---|---|
| **Covered by E35.2** (live-SQL RLS) | P-19, P-23, P-25, P-27, M-1 |
| **Covered by E35.3** (input/XSS/prompt-injection) | P-22, P-17, A-3 (harness) |
| **Covered by E35.4** (no-leak journey) | P-13, P-15, P-16, P-17, C-6, C-7, C-8, C-9, A-2, A-4, A-5 |
| **Covered by E35.5** (headers + JSON-LD + SEO) | P-1, P-3 |
| **Covered by E35.6** (AI-attack + anti-automation) | M-2, M-3, A-3 |
| **Deferred to E36** (DSR self-service automation) | P-18, P-26, P-28 |
| **Deferred to E38** (retention crons) | P-4, P-6, P-7, P-20 |
| **Deferred to E40** (cookie alignment) | X-1, X-2, X-3, X-4, X-5, C-5 |
| **Out-of-code / policy statement** | P-2, P-9, P-10, P-21, P-24, A-6, M-4 |

### Headline gaps — resolution log

**Resolved by E38** (merged via PR #57): The four retention rows
(P-4, P-6, P-7, P-20) collapsed to one root cause — Supabase Free tier
has no `pg_cron`. Resolution: GitHub Actions scheduled workflow
calling three RPCs (`purge_expired_attempts`,
`anonymize_expired_anticheat`, `anonymize_expired_edu_respondents`)
daily at 03:00 UTC. `/privacy` s5 copy updated; no `CONSENT_VERSION`
bump because the data model and processors are unchanged.

**Resolved by E40** (merged via PR #58): The five undeclared storage
writes (X-1..X-5) were collapsed to two declared rows on `/cookies`
— a new `locale` row under the "necessary" category (ePrivacy
strictly-necessary exemption for language selection), and the
existing `prefs` row populated with the four UI-state keys (sidebar,
blog pillars, dashboard intro, profile-completion dismiss). The four
UI-state writes are now gated by `hasConsent(record, "preferences")`
so a user rejecting preferences gets in-memory-only behaviour.
`CONSENT_VERSION` bumped 1.5.0 → 1.6.0 so existing users re-decide.

**Resolved by E42** (this PR): The remaining 14 `⚠️` rows — partial
implementations, unverified claims, out-of-code policy assertions
— are all closed. C-9 DNT/GPC respect was a real implementation gap
discovered during the re-audit (the `/cookies` copy promised it but
the code didn't honour it); P-18 + P-28 needed a fresh `export_my_data()`
RPC + `/api/account/export-data` endpoint + `DataExportCard` UI to
turn GDPR Art. 15 + Art. 20 from "manual admin queue" into real
self-service rights. A-6 (charity claim) was re-audited as recon
speculation, but the proactive `public/transparency.json` registry
was kept as future-proofing infra. M-4 (magic link TTL) was off by
one hour in the recon; the actual i18n value "1 hodina" matches the
Supabase default and is now locked.

**Compliance is at 100 % ✅.** Future work is incremental hardening
(E39 RPC zod + CSP nonce; E41 security headers on API responses
flagged by ZAP) rather than gap closure.

The five `❌` cross-cutting rows (X-1 to X-5) collapse to **one root cause**: no automated test asserts that "code-level cookie / localStorage writes" ⊆ "declared cookie / localStorage table". E35.4 will close that loop with a no-leak journey assertion that compares observed cookies + localStorage keys against an allowlist.

## Definition of Done

See [`tasks/DEFINITION_OF_DONE.md`](../DEFINITION_OF_DONE.md) § 2.
