# E38.6 — `/privacy` copy refresh + E35.1 matrix flip

**Epic:** [E38 — Retention crons + auto-anonymisation](../PLAN-2026-05-20-E38-retention-crons.md)
**Effort:** `XS` (≤ 2 h)
**Priority:** `P2`
**Status:** 🟡 Ready

## Context

After E38.1–E38.5 land, the legal copy on `/privacy` is again the
source of truth — but two strings need a touch-up:
- s5 says "naplánovaná denná úloha (Postgres pg_cron, runs 03:17 UTC)"
  — we no longer use pg_cron (PLAN D1). Replace with
  "naplánovaná denná úloha o 03:00 UTC".
- s5 cites migration `20260426110000_*` — keep but note new functions.

Plus we close the loop on the E35.1 matrix by flipping the affected
rows from ⚠️/❌ → ✅.

No `CONSENT_VERSION` bump because the data model and processors are
unchanged (PLAN D7) — only the scheduler implementation detail moved.

## Acceptance criteria

- **AC-1**: `src/i18n/locales/sk/legal.json` s5 retention section
  text updated to reflect 03:00 UTC + GH Actions cron (without
  exposing internal infra names — just "denná úloha o 03:00 UTC").
- **AC-2**: Mirror changes in `src/i18n/locales/cs/legal.json` and
  `src/i18n/locales/en/legal.json` (the other two locales must match).
- **AC-3**: Update `tasks/stories/E35.1-compliance-audit.md`:
  - P-4 status ⚠️ → ✅, evidence cites new migration.
  - P-6 status ❌ → ✅, evidence cites `anonymize_expired_anticheat`.
  - P-7 status ❌ → ✅, evidence cites
    `anonymize_expired_edu_respondents`.
  - P-20 status ❌ → ✅, evidence cites GH Actions workflow.
- **AC-4**: Update the matrix summary counts.
- **AC-5**: No `CONSENT_VERSION` bump (PLAN D7).

## Subtasks

### Implementation
- [ ] Edit the three legal.json files for s5.
- [ ] Edit E35.1 story file matrix rows + summary.

### Tests
- [ ] `npm test` to confirm `tests/security/seo-meta.test.ts` (which
      asserts meta description length) still passes after the copy
      edit.

### Documentation
- [ ] CHANGELOG line under `[Zmenené]`: "Drobná oprava textu v
      `/privacy` — denná retention úloha o 03:00 UTC."

### Code review (fresh context)
- [ ] Reviewer confirms no `CONSENT_VERSION` change.
- [ ] Reviewer confirms all three locales match.

## Dependencies

- Last story of the epic. Blocked by E38.1 + E38.2 + E38.3 shipping.

## Definition of Done

See [`tasks/DEFINITION_OF_DONE.md`](../DEFINITION_OF_DONE.md) § 2.
