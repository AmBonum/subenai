# E38.5 — DEPLOY_SETUP.sql + operator runbook update

**Epic:** [E38 — Retention crons + auto-anonymisation](../PLAN-2026-05-20-E38-retention-crons.md)
**Effort:** `XS` (≤ 2 h)
**Priority:** `P2`
**Status:** 🟡 Ready

## Context

`DEPLOY_SETUP.sql` is the standalone bootstrap file that mirrors all
migrations for an operator setting up a fresh Supabase instance. Per
CLAUDE.md: "Migrations live at `supabase/migrations/{timestamp}_{name}.sql`
and must also land in `DEPLOY_SETUP.sql`."

This story coordinates the sync after E38.1 + E38.2 land, plus a brief
operator note about the new GH Actions cron in the runbook.

## Acceptance criteria

- **AC-1**: `DEPLOY_SETUP.sql` includes the two new function
  definitions (verbatim from the migrations) in chronological order.
- **AC-2**: New section in `tasks/E38-runbook.md` (English, per
  CLAUDE.md language rule):
  - "What ships: 3 retention RPCs + GH Actions daily cron."
  - "What the operator needs: confirm `SUPABASE_SERVICE_ROLE_KEY`
    secret is in repo settings (should already exist from E35.7)."
  - "How to verify after merge: trigger
    `workflow_dispatch` on `retention-cron.yml`, check the log shows
    three RPC counts."
- **AC-3**: Mention in the runbook that pg_cron is NOT used (decision
  D1 of the plan) so a future operator doesn't try to enable it.

## Subtasks

### Implementation
- [ ] Sync new function bodies into `DEPLOY_SETUP.sql`.
- [ ] Write `tasks/E38-runbook.md`.

### Tests
- [ ] None — documentation + bootstrap-script sync.

### Documentation
- [ ] This story IS the documentation update.

### Code review (fresh context)
- [ ] Reviewer diff-checks `DEPLOY_SETUP.sql` vs the new migrations.

## Dependencies

- Blocked by E38.1 + E38.2 + E38.3 (need the actual ship surface).

## Definition of Done

See [`tasks/DEFINITION_OF_DONE.md`](../DEFINITION_OF_DONE.md) § 2.
