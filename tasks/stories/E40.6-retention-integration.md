# ~~E40.6 — Retention cron extension + privacy copy + CHANGELOG + feature flag flip~~ ✅

**Epic:** [E40 — DPA automation](../PLAN-2026-05-20-E40-dpa-automation.md)
**Effort:** `S` (≤ ½ day)
**Priority:** `P2`
**Status:** ✅ Done (2026-05-20)

## Deviations from draft AC

- **`.env.example` flipped to `true`** for local dev defaults, BUT the code semantics (`=== "true"`) mean production CF Pages remains OFF until operator explicitly sets the env var. `.env.example` does not get read by Vite at build time — it's documentation. Result: merging this PR does NOT auto-enable the flow in production.
- **Two-phase flag flip** documented in [E40-runbook.md](../E40-runbook.md): flow flag `VITE_DPA_FLOW_ENABLED` is separate from the draft watermark flag `VITE_DPA_TEMPLATE_DRAFT_WATERMARK`. Operator can enable the flow with watermark visible BEFORE legal sign-off (for stakeholder demos), then flip the watermark off after sign-off.
- **Resend already in /privacy s4** as a sub-processor — no edit needed there. Only added the new DPA row to the s3 processing table.
- **CHANGELOG entry** is a single long bullet announcing the full epic; it ships under "Pridané" matching the existing voice.
- **SchoolsGdprCard mailto fallback NOT removed** — kept as defensive code in case operator wants to disable temporarily. The new `tests/components/schools/SchoolsGdprCard.flag-on.test.tsx` asserts the on-state path via `vi.mock` of the feature-flag module.

## Context

Final polish. Three small but mandatory cleanups before the epic merges to `main` and the feature flag flips on for production traffic:

1. **Retention cron**: the existing E38 GitHub Actions workflow ([.github/workflows/retention-cron.yml](../../.github/workflows/retention-cron.yml)) calls three RPCs daily. Add a fourth call: `anonymize_expired_dpa_requests()` from E40.1.
2. **Privacy disclosure**: `/privacy` must declare that DPA requests are a new data flow. Add an entry to s4 (categories) + s5 (retention).
3. **Feature flag flip**: phase A shipped behind `VITE_DPA_FLOW_ENABLED=false`. After legal sign-off on the v0.1 template (out-of-code milestone), this story flips the flag to `true` in the Cloudflare Pages production env and updates `.env.example` accordingly.

## Acceptance criteria

- **AC-1**: `scripts/run-retention.mjs` (from E38) adds a fourth `.rpc('anonymize_expired_dpa_requests')` call. Output log shows the row count.
- **AC-2**: `tests/integration/security/retention.test.ts` (from E38) extends with a fourth scenario: seed a `dpa_requests` row with `created_at = now() - interval '13 months'`, run the script, assert `contact_name` + `contact_email` are NULL.
- **AC-3**: `src/routes/privacy.tsx` (or wherever the `/privacy` content lives) gets two new lines:
  - In s4 (kategórie údajov): "DPA žiadosti — meno kontaktu, e-mail kontaktu, názov školy. Účel: predzmluvné opatrenia podľa čl. 6 ods. 1 písm. b) GDPR."
  - In s5 (retencia): "DPA žiadosti — 12 mesiacov, potom automatická anonymizácia mena + e-mailu (názov školy ostáva pre štatistiku)."
- **AC-4**: `CHANGELOG.md` final epic-level entry for E40 (the per-story CHANGELOG lines from E40.1–E40.5 already in place):
  - "Automatizovaný DPA tok — žiadosť cez `/schools/dpa`, okamžité stiahnutie PDF + e-mailová kópia, admin prehľad v `/admin/dpa-requests`. Retencia 12 mes pre kontaktné údaje."
- **AC-5**: `DEPLOY_SETUP.sql` finalised (the migration from E40.1 already appended; confirm in this story).
- **AC-6**: Feature flag flip — `.env.example` line `VITE_DPA_FLOW_ENABLED=true` (default). Operator runs CF Pages env update manually (production + preview). Document the command in the runbook.
- **AC-7**: `SchoolsGdprCard.tsx` — once feature flag default is `true`, remove the mailto fallback branch (the dead branch). Keep the conditional import in case future ops disable temporarily, but unit test asserts default path goes to `/schools/dpa`.

## Subtasks

### Implementation
- [ ] `scripts/run-retention.mjs` — add fourth RPC call
- [ ] `tests/integration/security/retention.test.ts` — add fourth scenario
- [ ] `src/routes/privacy.tsx` — two new disclosure lines + Slovak copy in `i18n/locales/sk/marketing.json` (`privacy_*` keys)
- [ ] `CHANGELOG.md` — final epic entry
- [ ] `.env.example` — flip default
- [ ] [tasks/E40-runbook.md](./E40-runbook.md) — operator deployment checklist (CF Pages env update, Resend DKIM, legal sign-off log)

### Tests
- [ ] [Implementation #2 above]
- [ ] `tests/components/schools/SchoolsGdprCard.test.tsx` — default-flag-on case CTA → `/schools/dpa`.

### Documentation
- [ ] [implementation lines above]
- [ ] Update [PLAN-2026-05-20-E40-dpa-automation.md](../PLAN-2026-05-20-E40-dpa-automation.md) story-tracker rows → `✅ Done`.
- [ ] Update epic header status → `✅ DELIVERED YYYY-MM-DD`.

### Code review (fresh context)
- [ ] Reviewer confirms `/privacy` copy matches the actual flow (no overclaim, no underclaim).
- [ ] Reviewer confirms the retention RPC actually anonymises (cross-check against an integration test result).
- [ ] Reviewer confirms the feature-flag fallback removal doesn't accidentally break preview deploys where the env var might be missing (default to `true` if unset is fine; production overrides).

## Dependencies

- Blocked by E40.1–E40.5 (all stories must be green).
- External: **legal sign-off on v0.1 template** before the feature flag flip in production. Document the sign-off in the runbook.

## Definition of Done

See [`tasks/DEFINITION_OF_DONE.md`](../DEFINITION_OF_DONE.md) § 2 + § 3 (epic-level closure — Playwright spec from E40.5 must be green, privacy + CHANGELOG updated, lint/test/build clean, fresh-context CR done).
