# E45.18 — Playwright e2e for test detail editor (Phases 1+2+3 coverage)

**Epic:** [E45](../PLAN-2026-05-21-E45-test-detail-editor.md)
**Effort:** `L` (target) / `M` (actual — extended existing spec rather than greenfield)
**Status:** ✅ Done
**Commit prefix:** `feat(tests):`

## Akceptačné kritériá

- **AC-1:** `e2e/specs/app/test-editor.spec.ts` má TC-06 — TC-11
  (predtým TC-01 — TC-05 pokrývali základný editor):
  - **TC-06** Questions tab empty state na teste bez otázok
  - **TC-07** Order Mode toggle v Settings (fixed pre-selected default)
  - **TC-08** PasswordCard: submit disabled až do 8+ char + match
  - **TC-09** PasswordCard na lockovanom teste — Clear button + "set" status
  - **TC-10** Invite button na publikovanom teste je locked "Pripravujeme"
  - **TC-11** Invite button NIE je rendered na drafte
- **AC-2:** `e2e/poms/app/TestEditorPage.ts` rozšírený o všetky Phase 1+2+3
  selektory — `questionsRoot`, `orderMode*`, `password*`, `inviteButton`,
  `inviteComingSoonBadge`, `inviteTooltipTrigger`.
- **AC-3:** `e2e/seed/tests.ts` setuje defaults pre nové stĺpce
  (`question_order_mode: "fixed"`, `source_template_id: null`,
  `password_hash_version: 0`) tak, aby seed bol kompatibilný s reálnym
  schema.

## Lokálne overenie

E2E vyžaduje `VITE_SUPABASE_PROJECT_ID` v `.env`. Worktree štandardne
nemá .env — všetkých 11 TC pre tento spec zlyhalo lokálne s
`primeAuthSession: VITE_SUPABASE_PROJECT_ID is not set` (rovnako pre
predošlé TC-01 — TC-05, t. j. nie regresia z phase 4). CI Playwright job
spustí spec proti seedovanému prostrediu s nakonfigurovaným env.

## Definition of Done

- Spec syntax OK (lint 0/0)
- POM extension nemení existujúce getters (back-compat zachovaná)
- TC-01 — TC-11 sledujú rovnaký `test.describe` + `test.step` pattern
- 1997 unit + integration tests green (+2 z §M1 fix)
